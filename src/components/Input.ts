import { styled } from '@/styles';

const Input = styled('input', {
  p: 1,
  borderRadius: '$sm',
  border: '1px solid',
  borderColor: '$foreground',
  backgroundColor: '$bg',
  color: '$text',
});

export default Input;
